# GigaLib Platform Configuration
# Add/remove paths as needed for your system. All paths are checked — missing ones are skipped.

steam:
  paths:
    - "C:\\Program Files (x86)\\Steam"
    - "D:\\Program Files (x86)\\Steam"
    - "D:\\SteamLibrary"

ea:
  install_data:
    - "C:\\ProgramData\\EA Desktop\\InstallData"
  games_dirs:
    - "C:\\Program Files\\EA Games"

ubisoft:
  config_cache:
    - "C:\\Program Files (x86)\\Ubisoft\\Ubisoft Game Launcher\\cache\\configuration\\configurations"
  games_dirs:
    - "C:\\Program Files (x86)\\Ubisoft\\Ubisoft Game Launcher\\games"

xbox:
  # Xbox / Game Pass PC installs each game under a folder here that contains
  # Content\MicrosoftGame.config (StoreId + TitleId). Add extra drives if you
  # moved the install location in the Xbox app.
  install_dirs:
    - "C:\\XboxGames"
    - "D:\\XboxGames"
